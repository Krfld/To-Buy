import 'package:flutter/material.dart';
import 'package:to_buy/views/check_lists_view.dart';

class GroupsView extends StatelessWidget {
  const GroupsView({Key? key}) : super(key: key);

  // Navigator.push(context, MaterialPageRoute(builder: (context) => CheckListsView('currentGroupId')));

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
